blue = matrix(lena[,,3], ncol = 1)
)
# Perform CLARA clustering with 2 clusters
num_clusters <- 10
clustering <- clara(df, num_clusters, samples = 50)
clustering <- clara(df, num_clusters, samples = 50)
# Get cluster assignments
cluster_assignments <- clustering$clustering
# Get cluster medoids (centers)
cluster_medoids <- clustering$medoids
# Get cluster medoids (centers)
cluster_medoids <- clustering$medoids
df[,"red"] <- cluster_medoids[cluster_assignments, "red"]
df[,"green"] <- cluster_medoids[cluster_assignments, "green"]
df[,"blue"] <- cluster_medoids[cluster_assignments, "blue"]
G <- matrix(df$green, nrow = dim(lena)[1])
B <- matrix(df$blue, nrow = dim(lena)[1])
# Reconstitute the segmented image in the same shape as the input image
lena_segmented <- array(dim = dim(lena))
lena_segmented[,,1] <- R
lena_segmented[,,2] <- G
lena_segmented[,,3] <- B
# Display the original image and the segmented result
grid.arrange(
rasterGrob(lena),
rasterGrob(lena_segmented),
ncol = 2
)
# Display the original image and the segmented result
grid.arrange(
rasterGrob(lena),
rasterGrob(lena_segmented),
ncol = 2
)
setwd("E:/DADOS/VIEIRA/POS GRADUACAO/INFINET/CURSO/WORK_TRABALHO_5")
getwd()
setwd("E:/DADOS/VIEIRA/POS GRADUACAO/INFINET/CURSO/WORK_TRABALHO_5")
getwd()
```{r clara, echo=TRUE}
lena <- readPNG("Lenna.png")
# Reshape image into a data frame
df <- data.frame(
red = matrix(lena[,,1], ncol = 1),
green = matrix(lena[,,2], ncol = 1),
blue = matrix(lena[,,3], ncol = 1)
)
# Perform CLARA clustering with 2 clusters
num_clusters <- 10
clustering <- clara(df, num_clusters, samples = 50)
# Get cluster assignments
cluster_assignments <- clustering$clustering
# Get cluster medoids (centers)
cluster_medoids <- clustering$medoids
df[,"red"] <- cluster_medoids[cluster_assignments, "red"]
df[,"green"] <- cluster_medoids[cluster_assignments, "green"]
df[,"blue"] <- cluster_medoids[cluster_assignments, "blue"]
# Reshape the segmented image data
R <- matrix(df$red, nrow = dim(lena)[1])
G <- matrix(df$green, nrow = dim(lena)[1])
B <- matrix(df$blue, nrow = dim(lena)[1])
# Reconstitute the segmented image in the same shape as the input image
lena_segmented <- array(dim = dim(lena))
lena_segmented[,,1] <- R
lena_segmented[,,2] <- G
lena_segmented[,,3] <- B
# Display the original image and the segmented result
grid.arrange(
rasterGrob(lena),
rasterGrob(lena_segmented),
ncol = 2
)
library(tidyverse)
library(tidyverse)
library(rvest)
library(data.table)
library(robotstxt)
library(knitr)
library(dlookr)
library(dlookr)
library(readxl)
library(summarytools)
library(ggplot2)
library(fitdistrplus)
library(readr)
library(kableExtra)
library("Amelia")
library("mice")
library("corrplot")
library("ggpubr")
library(summarytools)
library(ggplot2)
library(fitdistrplus)
library(readr)
library(kableExtra)
library("Amelia")
library("mice")
library("corrplot")
library("ggpubr")
library("data.table")
library("ggpubr")
library("data.table")
library("plm")
library(readxl)
library(summarytools)
library(ggplot2)
library(fitdistrplus)
library(readr)
library(kableExtra)
library("Amelia")
library("mice")
library("corrplot")
library("ggpubr")
library("data.table")
library("plm")
library("shiny")
library(dlookr)
library(readxl)
library(summarytools)
library(ggplot2)
library(fitdistrplus)
library(readr)
library(kableExtra)
library("Amelia")
library("mice")
library("corrplot")
library("ggpubr")
library("data.table")
library("plm")
library("shiny")
library("flexdashboard")
library(dlookr)
library(readxl)
library(summarytools)
library(ggplot2)
library(fitdistrplus)
library(readr)
library(kableExtra)
library("Amelia")
library("mice")
library("corrplot")
library("ggpubr")
library("data.table")
library("plm")
library("shiny")
library("flexdashboard")
library("factoextra")
library("png")
library("jpeg")
library("cluster")
library("grid")
library("gridExtra")
setwd("E:/DADOS/VIEIRA/POS GRADUACAO/INFINET/CURSO/WORK_TRABALHO_5")
getwd()
## Executando com 3 cores:
```{r clara3, echo=TRUE}
lena <- readPNG("Lenna.png")
df <- data.frame(
red = matrix(lena[,,1], ncol = 1),
green = matrix(lena[,,2], ncol = 1),
blue = matrix(lena[,,3], ncol = 1)
)
# Perform CLARA clustering with 3 clusters
num_clusters <- 03
clustering <- clara(df, num_clusters, samples = 50)
# Get cluster assignments
cluster_assignments <- clustering$clustering
# Get cluster medoids (centers)
cluster_medoids <- clustering$medoids
df[,"red"] <- cluster_medoids[cluster_assignments, "red"]
df[,"green"] <- cluster_medoids[cluster_assignments, "green"]
df[,"blue"] <- cluster_medoids[cluster_assignments, "blue"]
# Reshape the segmented image data
R <- matrix(df$red, nrow = dim(lena)[1])
G <- matrix(df$green, nrow = dim(lena)[1])
B <- matrix(df$blue, nrow = dim(lena)[1])
# Reconstitute the segmented image in the same shape as the input image
lena_segmented <- array(dim = dim(lena))
lena_segmented[,,1] <- R
lena_segmented[,,2] <- G
lena_segmented[,,3] <- B
# Display the original image and the segmented result
grid.arrange(
rasterGrob(lena),
rasterGrob(lena_segmented),
ncol = 2
)
## Executando com 5 cores:
```{r clara5, echo=TRUE}
lena <- readPNG("Lenna.png")
df <- data.frame(
red = matrix(lena[,,1], ncol = 1),
green = matrix(lena[,,2], ncol = 1),
blue = matrix(lena[,,3], ncol = 1)
)
lena <- readPNG("Lenna.png")
df <- data.frame(
red = matrix(lena[,,1], ncol = 1),
green = matrix(lena[,,2], ncol = 1),
blue = matrix(lena[,,3], ncol = 1)
)
# Perform CLARA clustering with 5 clusters
num_clusters <- 05
clustering <- clara(df, num_clusters, samples = 50)
clustering <- clara(df, num_clusters, samples = 50)
# Get cluster assignments
cluster_assignments <- clustering$clustering
# Get cluster medoids (centers)
cluster_medoids <- clustering$medoids
df[,"red"] <- cluster_medoids[cluster_assignments, "red"]
df[,"green"] <- cluster_medoids[cluster_assignments, "green"]
df[,"blue"] <- cluster_medoids[cluster_assignments, "blue"]
# Reshape the segmented image data
R <- matrix(df$red, nrow = dim(lena)[1])
G <- matrix(df$green, nrow = dim(lena)[1])
B <- matrix(df$blue, nrow = dim(lena)[1])
# Reconstitute the segmented image in the same shape as the input image
lena_segmented <- array(dim = dim(lena))
lena_segmented[,,1] <- R
lena_segmented[,,2] <- G
lena_segmented[,,3] <- B
# Display the original image and the segmented result
grid.arrange(
rasterGrob(lena),
rasterGrob(lena_segmented),
ncol = 2
)
# Display the original image and the segmented result
grid.arrange(
rasterGrob(lena),
rasterGrob(lena_segmented),
ncol = 2
)
```
```{r clara10, echo=TRUE}
lena <- readPNG("Lenna.png")
df <- data.frame(
red = matrix(lena[,,1], ncol = 1),
green = matrix(lena[,,2], ncol = 1),
blue = matrix(lena[,,3], ncol = 1)
)
# Perform CLARA clustering with 2 clusters
num_clusters <- 10
clustering <- clara(df, num_clusters, samples = 50)
clustering <- clara(df, num_clusters, samples = 50)
# Get cluster assignments
cluster_assignments <- clustering$clustering
# Get cluster medoids (centers)
cluster_medoids <- clustering$medoids
df[,"red"] <- cluster_medoids[cluster_assignments, "red"]
df[,"red"] <- cluster_medoids[cluster_assignments, "red"]
df[,"green"] <- cluster_medoids[cluster_assignments, "green"]
df[,"blue"] <- cluster_medoids[cluster_assignments, "blue"]
# Reshape the segmented image data
R <- matrix(df$red, nrow = dim(lena)[1])
G <- matrix(df$green, nrow = dim(lena)[1])
# Get cluster assignments
cluster_assignments <- clustering$clustering
# Get cluster medoids (centers)
cluster_medoids <- clustering$medoids
df[,"red"] <- cluster_medoids[cluster_assignments, "red"]
df[,"green"] <- cluster_medoids[cluster_assignments, "green"]
df[,"blue"] <- cluster_medoids[cluster_assignments, "blue"]
# Reshape the segmented image data
R <- matrix(df$red, nrow = dim(lena)[1])
G <- matrix(df$green, nrow = dim(lena)[1])
B <- matrix(df$blue, nrow = dim(lena)[1])
# Reconstitute the segmented image in the same shape as the input image
lena_segmented <- array(dim = dim(lena))
lena_segmented[,,1] <- R
lena_segmented[,,2] <- G
lena_segmented[,,3] <- B
# Display the original image and the segmented result
grid.arrange(
rasterGrob(lena),
rasterGrob(lena_segmented),
ncol = 2
)
<
lena <- readPNG("Lenna.png")
df <- data.frame(
red = matrix(lena[,,1], ncol = 1),
green = matrix(lena[,,2], ncol = 1),
blue = matrix(lena[,,3], ncol = 1)
)
# Perform CLARA clustering with 2 clusters
num_clusters <- 20
clustering <- clara(df, num_clusters, samples = 50)
clustering <- clara(df, num_clusters, samples = 50)
# Get cluster assignments
cluster_assignments <- clustering$clustering
# Get cluster medoids (centers)
cluster_medoids <- clustering$medoids
df[,"red"] <- cluster_medoids[cluster_assignments, "red"]
df[,"red"] <- cluster_medoids[cluster_assignments, "red"]
df[,"green"] <- cluster_medoids[cluster_assignments, "green"]
df[,"blue"] <- cluster_medoids[cluster_assignments, "blue"]
# Reshape the segmented image data
R <- matrix(df$red, nrow = dim(lena)[1])
G <- matrix(df$green, nrow = dim(lena)[1])
B <- matrix(df$blue, nrow = dim(lena)[1])
# Reconstitute the segmented image in the same shape as the input image
lena_segmented <- array(dim = dim(lena))
lena_segmented[,,1] <- R
G <- matrix(df$green, nrow = dim(lena)[1])
B <- matrix(df$blue, nrow = dim(lena)[1])
# Reconstitute the segmented image in the same shape as the input image
lena_segmented <- array(dim = dim(lena))
lena_segmented[,,1] <- R
lena_segmented[,,2] <- G
# Display the original image and the segmented result
grid.arrange(
rasterGrob(lena),
rasterGrob(lena_segmented),
ncol = 2
)
lena <- readPNG("Lenna.png")
df <- data.frame(
red = matrix(lena[,,1], ncol = 1),
green = matrix(lena[,,2], ncol = 1),
blue = matrix(lena[,,3], ncol = 1)
)
# Perform CLARA clustering with 2 clusters
num_clusters <- 15
clustering <- clara(df, num_clusters, samples = 50)
clustering <- clara(df, num_clusters, samples = 50)
# Get cluster assignments
cluster_assignments <- clustering$clustering
# Get cluster assignments
cluster_assignments <- clustering$clustering
# Get cluster medoids (centers)
cluster_medoids <- clustering$medoids
df[,"red"] <- cluster_medoids[cluster_assignments, "red"]
df[,"green"] <- cluster_medoids[cluster_assignments, "green"]
df[,"blue"] <- cluster_medoids[cluster_assignments, "blue"]
# Reshape the segmented image data
R <- matrix(df$red, nrow = dim(lena)[1])
G <- matrix(df$green, nrow = dim(lena)[1])
B <- matrix(df$blue, nrow = dim(lena)[1])
# Reconstitute the segmented image in the same shape as the input image
lena_segmented <- array(dim = dim(lena))
lena_segmented[,,1] <- R
lena_segmented[,,2] <- G
lena_segmented[,,3] <- B
# Display the original image and the segmented result
grid.arrange(
rasterGrob(lena),
rasterGrob(lena_segmented),
ncol = 2
)
lena <- readPNG("Lenna.png")
df <- data.frame(
red = matrix(lena[,,1], ncol = 1),
green = matrix(lena[,,2], ncol = 1),
blue = matrix(lena[,,3], ncol = 1)
)
# Perform CLARA clustering with 2 clusters
num_clusters <- 18
clustering <- clara(df, num_clusters, samples = 50)
clustering <- clara(df, num_clusters, samples = 50)
# Get cluster assignments
cluster_assignments <- clustering$clustering
# Get cluster medoids (centers)
cluster_medoids <- clustering$medoids
df[,"red"] <- cluster_medoids[cluster_assignments, "red"]
df[,"green"] <- cluster_medoids[cluster_assignments, "green"]
df[,"blue"] <- cluster_medoids[cluster_assignments, "blue"]
# Reshape the segmented image data
R <- matrix(df$red, nrow = dim(lena)[1])
G <- matrix(df$green, nrow = dim(lena)[1])
G <- matrix(df$green, nrow = dim(lena)[1])
B <- matrix(df$blue, nrow = dim(lena)[1])
# Reconstitute the segmented image in the same shape as the input image
lena_segmented <- array(dim = dim(lena))
lena_segmented[,,1] <- R
lena_segmented[,,2] <- G
lena_segmented[,,3] <- B
# Display the original image and the segmented result
grid.arrange(
rasterGrob(lena),
rasterGrob(lena_segmented),
ncol = 2
)
lena <- readPNG("Lenna.png")
df <- data.frame(
red = matrix(lena[,,1], ncol = 1),
green = matrix(lena[,,2], ncol = 1),
blue = matrix(lena[,,3], ncol = 1)
)
# Perform CLARA clustering with 2 clusters
num_clusters <- 20
clustering <- clara(df, num_clusters, samples = 50)
clustering <- clara(df, num_clusters, samples = 50)
# Get cluster assignments
cluster_assignments <- clustering$clustering
# Get cluster medoids (centers)
cluster_medoids <- clustering$medoids
df[,"red"] <- cluster_medoids[cluster_assignments, "red"]
df[,"green"] <- cluster_medoids[cluster_assignments, "green"]
df[,"blue"] <- cluster_medoids[cluster_assignments, "blue"]
# Reshape the segmented image data
R <- matrix(df$red, nrow = dim(lena)[1])
G <- matrix(df$green, nrow = dim(lena)[1])
B <- matrix(df$blue, nrow = dim(lena)[1])
# Reconstitute the segmented image in the same shape as the input image
lena_segmented <- array(dim = dim(lena))
# Reconstitute the segmented image in the same shape as the input image
lena_segmented <- array(dim = dim(lena))
lena_segmented[,,1] <- R
lena_segmented[,,2] <- G
lena_segmented[,,3] <- B
# Display the original image and the segmented result
grid.arrange(
rasterGrob(lena),
rasterGrob(lena_segmented),
ncol = 2
)
lena <- readPNG("Lenna.png")
df <- data.frame(
red = matrix(lena[,,1], ncol = 1),
green = matrix(lena[,,2], ncol = 1),
blue = matrix(lena[,,3], ncol = 1)
)
# Perform CLARA clustering with 2 clusters
num_clusters <- 30
clustering <- clara(df, num_clusters, samples = 50)
clustering <- clara(df, num_clusters, samples = 50)
# Get cluster assignments
cluster_assignments <- clustering$clustering
# Get cluster medoids (centers)
cluster_medoids <- clustering$medoids
df[,"red"] <- cluster_medoids[cluster_assignments, "red"]
df[,"green"] <- cluster_medoids[cluster_assignments, "green"]
df[,"blue"] <- cluster_medoids[cluster_assignments, "blue"]
# Reshape the segmented image data
R <- matrix(df$red, nrow = dim(lena)[1])
G <- matrix(df$green, nrow = dim(lena)[1])
B <- matrix(df$blue, nrow = dim(lena)[1])
# Reconstitute the segmented image in the same shape as the input image
lena_segmented <- array(dim = dim(lena))
lena_segmented[,,1] <- R
lena_segmented[,,2] <- G
lena_segmented[,,3] <- B
# Display the original image and the segmented result
grid.arrange(
rasterGrob(lena),
rasterGrob(lena_segmented),
ncol = 2
)
lena <- readPNG("Lenna.png")
df <- data.frame(
red = matrix(lena[,,1], ncol = 1),
green = matrix(lena[,,2], ncol = 1),
blue = matrix(lena[,,3], ncol = 1)
)
# Perform CLARA clustering with 2 clusters
num_clusters <- 60
clustering <- clara(df, num_clusters, samples = 50)
clustering <- clara(df, num_clusters, samples = 50)
# Get cluster assignments
cluster_assignments <- clustering$clustering
# Get cluster medoids (centers)
cluster_medoids <- clustering$medoids
df[,"red"] <- cluster_medoids[cluster_assignments, "red"]
df[,"green"] <- cluster_medoids[cluster_assignments, "green"]
df[,"blue"] <- cluster_medoids[cluster_assignments, "blue"]
# Reshape the segmented image data
R <- matrix(df$red, nrow = dim(lena)[1])
G <- matrix(df$green, nrow = dim(lena)[1])
B <- matrix(df$blue, nrow = dim(lena)[1])
# Reconstitute the segmented image in the same shape as the input image
lena_segmented <- array(dim = dim(lena))
lena_segmented[,,1] <- R
lena_segmented[,,2] <- G
lena_segmented[,,3] <- B
# Display the original image and the segmented result
grid.arrange(
rasterGrob(lena),
rasterGrob(lena_segmented),
ncol = 2
)
lena <- readPNG("Lenna.png")
df <- data.frame(
red = matrix(lena[,,1], ncol = 1),
green = matrix(lena[,,2], ncol = 1),
blue = matrix(lena[,,3], ncol = 1)
)
# Perform CLARA clustering with 2 clusters
num_clusters <- 100
clustering <- clara(df, num_clusters, samples = 50)
clustering <- clara(df, num_clusters, samples = 50)
# Get cluster assignments
cluster_assignments <- clustering$clustering
# Get cluster medoids (centers)
cluster_medoids <- clustering$medoids
df[,"red"] <- cluster_medoids[cluster_assignments, "red"]
df[,"green"] <- cluster_medoids[cluster_assignments, "green"]
df[,"blue"] <- cluster_medoids[cluster_assignments, "blue"]
# Reshape the segmented image data
R <- matrix(df$red, nrow = dim(lena)[1])
G <- matrix(df$green, nrow = dim(lena)[1])
B <- matrix(df$blue, nrow = dim(lena)[1])
# Reconstitute the segmented image in the same shape as the input image
lena_segmented <- array(dim = dim(lena))
lena_segmented[,,1] <- R
lena_segmented[,,2] <- G
lena_segmented[,,3] <- B
# Display the original image and the segmented result
grid.arrange(
rasterGrob(lena),
rasterGrob(lena_segmented),
ncol = 2
)
